1.
```   
if i < 4:
    dv_hat = S @ v_hat + 50 * (v_real - v_hat) + 10 * filtered_v
else:
    dv_hat = S @ v_hat + 1 * filtered_v
```
这里的两个参数怎么设计，怎么理解

1. 代理节点相关变量
在 Agent 类中，每个节点存储了自己的 系统矩阵、控制增益，它们的具体含义如下：
- self.A：倒立摆系统的状态矩阵
- self.B：倒立摆系统的输入矩阵
- self.K11：控制增益矩阵，控制倒立摆系统
- self.K12：控制增益矩阵，调节外部信号影响

系统状态变量

在 total_system(t, y) 里面：
y：整个系统的状态变量，维度是 8×6 的 一维数组
states：将 y 重新 reshape 成 8×6 形状，表示每个代理的状态：
- states[:, 0]：小车位置 x
- states[:, 1]：小车速度 ^x
- states[:, 2]：摆角 𝜃
- states[:, 3]：摆角速度 𝜃
- states[:, 4]：代理节点估计的信号 v1
- states[:, 5]：代理节点估计的信号 v2

filtered_v 代表 邻居的信号估计值，经过 RCP-f 过滤后的结果

当前问题：filter_v好像不能很好的控制信息收敛，检查filter_v的计算方法

必须要有v_real - v_hat这一项，不然单靠filter_v无法收敛
有很小的一段回升，结果不收敛于零